# OQ-0004

status: closed
title: Workers.dev Ops Access Ingress Reached Current-Head Attested Closure
opened date: 2026-04-02
owner: platform
affected IDs: `DEC-0003`,`OQ-0002`,`OQ-0004`,`OQ-0005`,`OQ-0006`,`CF-NET-003`,`CF-NET-004`,`CF-NET-005`,`CF-NET-006`,`CF-NET-007`,`DATA-D1-006`,`REQ-OPS-010`,`REQ-OPS-011`,`REQ-OPS-012`,`IF-OPS-001`,`IF-OPS-002`,`IF-OPS-003`,`IF-OPS-004`,`IF-OPS-005`,`IF-OPS-006`,`IF-OPS-007`,`IF-OPS-008`,`TEST-COST-001`,`TEST-DER-001`,`TEST-OPS-001`,`EVID-DER-001`,`EVID-OPS-001`
next review date: 2026-04-30
closed date: 2026-04-02
resolved by: `DEC-0003`

## Question

本问题提出时，仓库已经有一条明确的 workers.dev Access 候选 contract，但还缺 current-head attested proof。`2026-04-02T19:47:44Z` 完成的 GitHub Actions run `23918727505` 证明了 `staging` / `pre-release` 的 Access ingress 已经真实可达，却同时把 active blocker 收敛成了 Cloudflare managed D1 不接受 raw transaction SQL 的 rebuild 失败。

该不确定性现已由 [DEC-0003](/root/Matrix/spec/decisions/DEC-0003.md) 收敛。GitHub Actions run `23923285726`（created `2026-04-02T21:40:13Z`，updated `2026-04-02T21:45:53Z`，code state `6fc23f493b5a19cfc2ca20993cc0772ee06a8754`）完成了以下闭环：

* `ci-integration`、`staging`、`pre-release` 三个 non-local job 全部成功；
* `staging` / `pre-release` 的 `Prepare non-local ops Access session` 均成功；
* 两环境都通过 bounded readiness probe，并在 Access 保护下完成 environment-backed suite；
* 两环境都上传了 raw bundle 到 R2、生成并上传了 `EnvironmentRunAttestation`；
* `phase08-evidence-l1-20260402T214017Z-23923285726-1` evidence artifact 中，`EVID-DER-001` 已真实转为 `pass`，其 provenance 分别回链到
  `r2://matrix-evidence-staging/gha/23923285726/1/staging/20260402T214017Z/run-bundle.tgz`
  和
  `r2://matrix-evidence-pre-release/gha/23923285726/1/pre-release/20260402T214017Z/run-bundle.tgz`。

因此，本问题的 closure 现在是明确的：

* workers.dev Access path 对 `staging` / `pre-release` 已经不是 placeholder 或“仅本地自洽”的候选，而是被当前代码状态的 attested run 证明可用；
* `ops-worker` 通过真实 Access ingress 收到并消费了可验证的 `Cf-Access-Jwt-Assertion`，且 `/_ops/v1/rebuilds` 不再被 managed-D1 raw transaction SQL 卡住；
* `TEST-DER-001` / `EVID-DER-001` 的 Access-authenticated rebuild consistency 要求已经被 current-head run 证明；
* `TEST-OPS-001` / `EVID-OPS-001` 的未完成原因不再属于 Access topology；后续 same-head raw proof 已由 [OQ-0005](/root/Matrix/spec/open-questions/OQ-0005.md) 关闭 rollout-skew question，而当前 remaining gap 已转为 [OQ-0006](/root/Matrix/spec/open-questions/OQ-0006.md) 的 cost gate 挡住 shared pre-release attestation。

## Why It Matters

* [21-runtime-topology-and-platform-model.md](/root/Matrix/spec/framework/21-runtime-topology-and-platform-model.md) 与 [22-data-consistency-and-routing.md](/root/Matrix/spec/framework/22-data-consistency-and-routing.md) 已把 `/_ops/*` 固定为 Access-protected management ingress，而不是普通公网 surface。
* [40-security-and-abuse-resistance.md](/root/Matrix/spec/framework/40-security-and-abuse-resistance.md) 与 [23-interface-contract-catalog.md](/root/Matrix/spec/framework/23-interface-contract-catalog.md) 要求 `ops-worker` 只信任经 Access 生成的 JWT；run `23923285726` 现已把这条要求从“repo-side contract”推进为 current-head attested fact。
* `TODO.md` 的 `08.04` / `08.05` 现在必须把 Access topology 与 rollout-skew / cost proof 两类 blocker 分开处理；继续把 `TEST-OPS-001` 或 `TEST-COST-001` 的未完成原因写成 Access 未闭合，会夸大真实 blocker。

## Options

1. Keep the current workers.dev Access automation path and obtain current-head attested proof
   继续沿用当前 repo 已实现的 workers.dev Access candidate contract，但必须通过新的 GitHub Actions attested run 证明其在真实账号上可工作。

2. Abandon workers.dev Access and move to an active-zone management hostname
   为 `ops-worker` 引入真实 management domain、Access application、service token / policy，并让 workflow 在 Access 边缘完成鉴权，到达 `ops-worker` 时表现为可验证 JWT。

3. Narrow the product/test boundary by decision
   通过新的 `DEC-ID` 正式把 `TEST-DER-001` rebuild consistency 或 `TEST-OPS-001` non-local gate 从当前 Phase 08/L1 目标中移出，明确承认当前产品边界不提供这类 positive non-local proof。

## Current Recommendation

本问题现已关闭，当前唯一有效建议是：

* 继续把 workers.dev Access path 视为 `staging` / `pre-release` 的已证明 management ingress；
* 不要把本问题的 closure 误写成 `EVID-OPS-001` 已闭环；后续 same-head raw proof 已关闭 [OQ-0005](/root/Matrix/spec/open-questions/OQ-0005.md)，但 shared pre-release attestation 仍被 [OQ-0006](/root/Matrix/spec/open-questions/OQ-0006.md) 阻断；
* 若未来 workers.dev Access contract、Cloudflare Access 文档语义或 attested path 再次发生漂移，再通过新的 `OQ-ID` 重新打开问题，而不是回退成 placeholder 叙述。

## Closure Criteria

本问题已关闭，闭环条件如下：

* 来自当前代码状态的 GitHub Actions attested run `23923285726` 已证明 workers.dev Access automation 真正闭合；
* `staging` / `pre-release` 的 `/_ops/v1/healthz` 与 `/_ops/v1/rebuilds` 已经在 Access 保护下成功执行；
* `EVID-DER-001` 已在同轮 evidence artifact 中真实转为 `pass`；
* `TODO.md` 与 source-observation register 已同步更新为“Access topology 已闭环，剩余 blocker 为 `OQ-0006` / `OQ-0002`，而不是 Access topology 或 rollout-skew question 本身”的真实状态。

# OQ-0005

status: closed
title: Non-local Rollout-Skew Proof Via Cloudflare Gradual Deployments
opened date: 2026-04-02
owner: platform
affected IDs: `OQ-0005`,`CF-WKR-012`,`CF-WKR-014`,`CF-WKR-028`,`CF-DO-005`,`CF-DO-021`,`REQ-OPS-010`,`REQ-OPS-011`,`REQ-OPS-012`,`IF-OPS-009`,`DATA-OPS-012`,`FLOW-OPS-ROLLOUT-SKEW`,`TEST-OPS-001`,`EVID-OPS-001`
next review date: 2026-07-03
closed date: 2026-04-04
resolved by: `DEC-0004`

## Question

`TEST-OPS-001` 要求 pre-release 非本地门禁真实证明 `new Worker -> old DO` 与 `old Worker -> new DO` 兼容性。到 `2026-04-04` 为止，dual-version deployment contract、dedicated canonical suite、strict evidence mapping 和 Access-protected `/_ops` ingress 都已经落地；此前 remaining question 只是：在关闭本问题时被复核的 pushed head 是否还能在 Cloudflare 官方 gradual deployment + named-environment topology 下，真实拿到 rollout-skew proof。

这条问题现已由 [DEC-0004](/root/Matrix/spec/decisions/DEC-0004.md) 收口，其依据固定为关闭当时 latest reviewed pushed-head run `23986289866`（head `b6b3789eebf51f853cc5b96f295890e644cef553`）的 same-head remote artifact。当前分支 HEAD 已在同日后续前推到 docs-only `27959472aceeef5be5611ef910e67c4ceb085090`，但该提交尚未重新触发 non-local rerun，因此这里的 closure basis 仍不是“当前分支 head 的 remote truth”，而是上述 latest reviewed runtime/non-local head 的 proof：

1. `local-foundation` 成功，说明此前 `23985777845` 的 harness parser regression 已被修复，workflow 能再次真实进入 non-local path。
2. `nonlocal (pre-release)` 成功完成 `Prepare non-local ops Access session`、`Start pre-release rollout skew deployment`、`Run environment-backed suite`、`Restore pre-release rollout skew deployment`、raw bundle upload、R2 upload 与 provenance write。
3. raw `pre-release.log` 明确记录：
   * `TEST-OPS-001 pre-release proves worker/authority rollout skew compatibility through Access-protected ops probe` 为 `ok`；
   * `TEST-DER-001`,`TEST-MEDIA-001`,`TEST-SEC-001` 也都为 `ok`；
   * 唯一 failing test 是 `TEST-COST-001`，并按 [OQ-0006](/root/Matrix/spec/open-questions/OQ-0006.md) 继续 `500/internal` fail-closed。
4. raw `pre-release.json` 与 `rollout-skew-probe.json` 进一步给出该 reviewed head 的 same-head proof payload：
   * `rollout_skew_probe` sidecar 已真实存在；
   * `dual_version_deployment_id`、baseline/candidate gateway official version IDs 与 tags 都被记录；
   * `new_worker_old_authority` 与 `old_worker_new_authority` 两个 assertions 都为 `true`；
   * `observations[]` 已真实记录 `new Worker -> old UserDO/RoomDO` 与 `old Worker -> new UserDO/RoomDO` pairing。

因此，当前 unresolved state 已不再是 rollout-skew contract / Cloudflare gradual deployment viability 问题。本轮没有生成 pre-release attestation，原因也已在 same-head raw artifact 中被精确收敛为 cost gate：`Suite artifact parsing failed: pre_release_cost_observation sidecar is required when TEST-COST-001 is covered`。换言之，`EVID-OPS-001` 目前仍然 `fail`，但它现在 fail 的原因已经不是 rollout-skew proof 缺失，而是 shared pre-release attestation 仍被 [OQ-0006](/root/Matrix/spec/open-questions/OQ-0006.md) 阻断。

## Why It Matters

* [42-deployment-migration-and-recovery.md](/root/Matrix/spec/framework/42-deployment-migration-and-recovery.md) 把 `REQ-OPS-010`,`REQ-OPS-011`,`REQ-OPS-012` 固定为 rollout compatibility gate，而不是 deployment smoke。
* [43-testing-and-compliance.md](/root/Matrix/spec/framework/43-testing-and-compliance.md) 把 `TEST-OPS-001` 定义为 pre-release 非本地门禁；当前 same-head raw artifact 已证明 dedicated canonical suite 和 dual-version probe 可以在真实 Cloudflare topology 下工作。
* [44-verification-and-evidence-register.md](/root/Matrix/spec/framework/44-verification-and-evidence-register.md) 仍要求 `EVID-OPS-001` 消费 attested pre-release report；但本轮剩余 attestation 缺口已经被证明属于 cost gate，而不是 rollout-skew gate。

## Current Recommendation

本问题已关闭，后续处理方式是：

* 不再把 `TEST-OPS-001` 的 remaining gap 写成 rollout-skew contract 未闭环；
* 把 `EVID-OPS-001` 当前仍为 `fail` 的原因诚实归类到 shared pre-release attestation 缺失；
* 将 active blocker 继续收敛到 [OQ-0006](/root/Matrix/spec/open-questions/OQ-0006.md) 与 `TODO.md` 的 `08.04D` / `08.05`，而不是重复保留一个已经被 same-head remote artifact 回答的问题。

## Closure Criteria

本问题现已关闭，闭环依据如下：

* `2026-04-04` 用于关闭本问题的 pushed head `b6b3789eebf51f853cc5b96f295890e644cef553` 的 same-head GitHub Actions run `23986289866` 已真实执行 `tests/pre-release/test-ops-001.test.mjs` 并得到 `pass`；
* same-head raw artifact 已真实产出 `rollout_skew_probe.json`，且两个 required assertions 都为 `true`；
* remaining missing pre-release attestation 已被证明是 `TEST-COST-001` / `pre_release_cost_observation` blocker，而不是 rollout-skew proof 缺失。

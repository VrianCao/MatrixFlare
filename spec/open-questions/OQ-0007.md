# OQ-0007

status: closed
title: GitHub workflow_dispatch Default-Branch Restriction Blocks Same-Head Proof For New Production Automation Workflows
opened date: 2026-04-05
owner: platform
affected IDs: `OQ-0007`,`REQ-OPS-003`,`TEST-OPS-003`,`EVID-OPS-003`,`DEC-0005`,`FLOW-OPS-PROD-CANDIDATE`,`FLOW-OPS-PROD-INSTALL`,`FLOW-OPS-PROD-PROMOTE`,`FLOW-OPS-PROD-ROLLBACK`,`FLOW-OPS-PROD-COST-SNAPSHOT`
next review date: 2026-07-05
closed date: 2026-04-05
resolved by: `DEC-0005`

## Question

本问题打开时的核心判断是：production automation workflows 还只存在于 feature branch，因此 GitHub Actions 的 `workflow_dispatch` 默认分支约束让 same-head remote proof 根本无法启动。

截至 `2026-04-05`，这条判断已被同日 same-head remote truth 直接反证，不能继续保留为 active blocker：

1. repository default branch 仍是 `master`，但 `origin/master` 已真实包含 `.github/workflows/prod-install.yml`、`.github/workflows/release-candidate.yml`、`.github/workflows/promote-prod.yml`、`.github/workflows/rollback-prod.yml`、`.github/workflows/prod-cost-monthly.yml`。
2. current head `ec1b647e55b7fbb571145cc4a3c5acd43beb99b6` 已真实获得 same-head GitHub Actions runs：
   * `prod-install` 成功：run `24000789563`
   * `release-candidate` 已可 dispatch，并在保留 raw blocker artifact 后 fail-closed：run `24001043537`
   * `promote-prod` 已可 dispatch，并在保留 raw blocker artifact 后 fail-closed：run `24001065315`
   * `rollback-prod` 已可 dispatch，并在保留 raw blocker artifact 后 fail-closed：run `24001077197`
   * `prod-cost-monthly` 已可 dispatch，并在保留 raw blocker artifact 后 fail-closed：run `24000831481`
3. `prod-install` same-head 成功 run `24000789563` 还进一步证明 target account 中的 fixed-name production topology 已被当前 head 真实建立，而不是仍停留在“workflow 只能本地想象”的状态：
   * `matrix-gateway-worker-prod`
   * `matrix-jobs-worker-prod`
   * `matrix-ops-worker-prod`
   * Access-protected prod ops URL `https://matrix-ops-worker-prod.yevdndjyebksug.workers.dev`

因此，当前 `08.05A` / `TEST-OPS-003` / `EVID-OPS-003` 已不再受 “workflow_dispatch default-branch restriction blocks same-head proof” 这条问题约束。production automation remaining gaps 现在只属于 downstream workflow inputs / cost evidence truth，而不再属于 GitHub Actions 入口不可用。

## Why It Matters

* [42-deployment-migration-and-recovery.md](/root/Matrix/spec/framework/42-deployment-migration-and-recovery.md) 把 GitHub Actions 固定为 production automation 的执行入口；一旦 same-head dispatch 已被真实证明可用，就不能继续把入口 blocker 留成 open。
* [44-verification-and-evidence-register.md](/root/Matrix/spec/framework/44-verification-and-evidence-register.md) 要求 `EVID-OPS-003` 根据 same-head remote proof 或 raw blocker artifacts 诚实判定；当前 same-head runs 已存在，因此“因为 default branch 限制所以完全不能跑”已不再是事实。
* 若继续保留本问题 open，就会把 production automation 的 active blocker 错写成 GitHub 平台限制，掩盖真实剩余问题已经转移到 reviewed candidate inputs 与 `OQ-0002` 成本证据。

## Current Recommendation

本问题现已关闭，后续处理方式是：

* 不再把 `08.05A` 的 remaining work 写成 default-branch dispatch blocker；
* 将 same-head remote proof 固定为当前已审计 runs：`24000789563`,`24001043537`,`24001065315`,`24001077197`,`24000831481`；
* 把 active cost blocker 继续收敛到 [OQ-0002](/root/Matrix/spec/open-questions/OQ-0002.md) 与 [OQ-0006](/root/Matrix/spec/open-questions/OQ-0006.md)，而不是重复保留一个已被 remote truth 关闭的问题。

## Closure Criteria

本问题现已关闭，闭环依据如下：

* relevant production automation workflow definitions 已真实存在于 repository default branch；
* current head `ec1b647e55b7fbb571145cc4a3c5acd43beb99b6` 已真实拿到 same-head `prod-install` / `release-candidate` / `promote-prod` / `rollback-prod` / `prod-cost-monthly` remote runs；
* `prod-install` 已真实建立 fixed-name production topology；
* 其余 workflows 在当前输入不足时也已真实按 raw blocker artifact retention contract fail-closed，而不是卡在 dispatch 不可执行。

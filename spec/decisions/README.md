# Decision Register

本目录用于存放规范级决策记录。

约束：

* 文件名必须是 `<DEC-ID>.md`，例如 `DEC-0001.md`。
* 每个决策必须至少包含：status、title、owner、affected IDs、decision、rationale、approved date、expiry/review date。
* 所有 `waived` evidence、规范冲突裁决、恢复例外都必须引用本目录中已存在的 `DEC-ID`。
